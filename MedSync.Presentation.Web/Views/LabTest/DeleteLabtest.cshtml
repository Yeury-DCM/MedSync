@model MedSync.Core.Application.ViewModels.LabTests.LabTestViewModel
@{
    ViewData["Title"] = "Eliminar Usuario";
}

<div class="row">
    <div class="col-6 offset-3">
        <div class="card">
            <div class="card-header bg-danger text-light ">
                <h4>¿Estás seguro que quieres elimr el usuario @Model.Name?</h4>
            </div>
            <div class="card-body bg-light text-dark">

                <form method="post" asp-controller="LabTest" asp-action="DeletePost">

                    <input type="hidden" asp-for="Id" />

                    <button type="submit" class="btn btn-danger">Eliminar</button>
                    <a asp-controller="LabTest" asp-action="Index" class="btn btn-warning">Cancelar</a>
                </form>
            </div>
        </div>
    </div>
</div>
